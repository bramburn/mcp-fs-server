# Git / editor cruft
.git
.vscode
.vscode-test
coverage
*.log

# Source & tests – runtime only needs 'out' + 'resources' + 'bin'
src/**
test/**
results.json

# Build / tooling configs not needed at runtime
tsconfig.json
vitest.config.ts
vite.config.ts
svelte.config.js
tailwind.config.js
postcss.config.js
eslint.config.mjs
components.json
idea.md

# Rust sources (binaries are kept from bin/)
rust

# Node modules – safe to exclude because host + webview are bundled
node_modules

# VSIX build artifacts
bin/**/*.vsix

# Keep only the necessary binaries
!bin/clipboard-monitor*
!bin/clipboard-files*

# Keep WASM resources
!resources/
!resources/*.wasm

# From out/, only keep the bundled extension and webview
out/**
!out/extension.cjs
!out/extension.cjs.map
!out/webview/**
